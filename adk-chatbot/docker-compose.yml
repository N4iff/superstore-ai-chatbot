services:
  chatbot:
    build: .
    restart: unless-stopped
    ports:
      - "8001:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - model_cache:/root/.cache

volumes:
  model_cache:
